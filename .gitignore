# macOS
.DS_Store
.AppleDouble
.LSOverride

# Local working files (not for the repo)
claude/
node_modules/
package.json
package-lock.json

# Environment variables (API keys — never commit)
app/.env
.env
